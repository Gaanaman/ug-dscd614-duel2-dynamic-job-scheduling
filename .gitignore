__pycache__/
*.py[cod]
.venv/
venv/
.env
.ipynb_checkpoints/
.DS_Store
.pytest_cache/
*.egg-info/

# Large datasets are never committed -- provide a script that obtains them.
data/

# logs/ and figures/ ARE committed. Every figure in the report must trace back
# to a committed log, so do not add them here.

# built submission PDFs (regenerated by scripts/build_submission.sh)
submission/

# third-party paper text used only for prose calibration; not ours to redistribute
corpus/
.prose-corpus

# LaTeX build artefacts
paper/*.aux
paper/*.log
paper/*.out
paper/*.bbl
paper/*.blg
paper/main.pdf
